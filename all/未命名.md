```mermaid
flowchart TD     A[输入: 查询文本] --> B[嵌入模型: 生成查询向量]     B --> C[意图识别子模块]     C -->|闲聊意图| D[路由: 闲聊模型]     C -->|问答意图| E[向量相似性评估子模块 ES 8.12]     E --> F{相似度 > 阈值?}     F -->|是| G[路由: ES 8.12 向量数据库]     F -->|否| H[规则匹配子模块]     H --> I{包含生成关键词?}     I -->|是| J[路由: 大模型 API]     I -->|否| K[路由: 大模型 API]     B --> H     D --> L[决策融合子模块]     G --> L     J --> L     K --> L     L --> M[输出: 路由标签 + 附加数据]
```